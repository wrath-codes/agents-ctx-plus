app:
  name: zenith
  note: null
  features:
    auth: true
    billing: false
  db:
    timeout_ms: 2500
    pool:
      min: 2
      max: 20
  retries: [1, 2, 3]
routes:
  - path: /health
    method: GET
  - path: /users
    method: POST
